cmake_minimum_required(VERSION 3.18)
project(gravitational-box-gpu LANGUAGES CXX CUDA)

# This file is intentionally not used by the top-level build.
# The top-level `CMakeLists.txt` builds both CPU and GPU variants.

